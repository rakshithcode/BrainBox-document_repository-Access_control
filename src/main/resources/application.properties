# === Database Configuration ===
spring.datasource.url=jdbc:mysql://localhost:3306/brainbox_db?useSSL=false&serverTimezone=UTC


spring.datasource.username=root
spring.datasource.password=Prath@123
# === JPA / Hibernate Configuration ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# === Disable automatic schema.sql / data.sql execution ===
spring.sql.init.mode=never

# Optional: Better dialect handling
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Optional: Prevent console warning for open-in-view
spring.jpa.open-in-view=false
logging.level.org.springframework.security=DEBUG




